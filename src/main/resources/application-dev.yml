#
server:
  port: 8090

spring:
  application:
    name: mcp-server
#  main:
#    web-application-type: none # 必须禁用web应用类型
#    banner-mode: off # 禁用banner
  ai:
    mcp:
      server:
#        stdio: true # 启用stdio模式
        name: mcp-server # 服务器名称

endpoint:
  mianshiya:
    searchQuestion: http://api.mianshiya.com/api/question/mcp/search
    resultLink: https://www.mianshiya.com/question/%s
